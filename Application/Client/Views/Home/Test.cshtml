@{
    ViewData["Title"] = "Dashboard";
    Layout = "LayoutHome";
}

<!-- If Admin, Then show Ticket List -->
<div class="card">
    <div class="card-body p-3">
        <div class="row">
            <div class="col-8">
                <div class="numbers">
                    @if (User.IsInRole("Admin"))
                    {
                        <p class="text-sm mb-0 text-uppercase font-weight-bold">Ticket List</p>
                        <!-- Insert Table Here -->
                    }
                    else
                    {
                        <p class="text-sm mb-0 text-uppercase font-weight-bold">My Tickets</p>
                        <div id="cardSelf">
                            <!-- Insert Cards Here -->
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>